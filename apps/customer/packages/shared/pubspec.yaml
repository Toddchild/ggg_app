name: shared
description: Shared models, services and widgets for GGG apps
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0

flutter:
  uses-material-design: true
