name: ggg_app
description: Go Garbage Grabber mobile/web app
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  image_picker: ^1.1.2
  url_launcher: ^6.3.2
  shared_preferences: ^2.3.2
  http_parser: ^4.0.2
  file_picker: ^8.3.7
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # If you have assets/fonts, re-add them here under flutter:
  # assets:
  #   - assets/
